// Copy of the Costs page (owned by its page package; F0 stub).
// Every key starts with 'costs.'. COPY = labels, answers, empty states, table headers, takeaways;
// DEFS = { short, long?, template? } for (i) hints (hintKey 'costs.<key>').

export const COPY = {
  'costs.title': "Costs",
  'costs.question': "How much do we spend, on what, and why is it growing?",
};

export const DEFS = {};
