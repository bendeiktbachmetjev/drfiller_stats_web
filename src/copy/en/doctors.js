// Copy of the Doctors page (owned by its page package; F0 stub).
// Every key starts with 'doctors.'. COPY = labels, answers, empty states, table headers, takeaways;
// DEFS = { short, long?, template? } for (i) hints (hintKey 'doctors.<key>').

export const COPY = {
  'doctors.title': "Doctors",
  'doctors.question': "Who uses Dr.Filler, who pays, and what does each doctor cost us?",
};

export const DEFS = {};
