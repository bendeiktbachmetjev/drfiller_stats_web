// Copy of the Models page (owned by its page package; F0 stub).
// Every key starts with 'models.'. COPY = labels, answers, empty states, table headers, takeaways;
// DEFS = { short, long?, template? } for (i) hints (hintKey 'models.<key>').

export const COPY = {
  'models.title': "Models",
  'models.question': "Which model answers, and is it fast and without failures?",
};

export const DEFS = {};
