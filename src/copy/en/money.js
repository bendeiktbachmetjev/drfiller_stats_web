// Copy of the Money page (owned by its page package; F0 stub).
// Every key starts with 'money.'. COPY = labels, answers, empty states, table headers, takeaways;
// DEFS = { short, long?, template? } for (i) hints (hintKey 'money.<key>').

export const COPY = {
  'money.title': "Money",
  'money.question': "How much money comes in, how much we keep, and will it pay off at 100–300 doctors?",
};

export const DEFS = {};
