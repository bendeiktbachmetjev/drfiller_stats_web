// Copy of the Overview page (owned by its page package; F0 stub).
// Every key starts with 'overview.'. COPY = labels, answers, empty states, table headers, takeaways;
// DEFS = { short, long?, template? } for (i) hints (hintKey 'overview.<key>').

export const COPY = {
  'overview.title': "Overview",
  'overview.question': "Is everything OK, and are we making money?",
};

export const DEFS = {};
