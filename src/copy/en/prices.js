// Copy of the Prices page (owned by its page package; F0 stub).
// Every key starts with 'prices.'. COPY = labels, answers, empty states, table headers, takeaways;
// DEFS = { short, long?, template? } for (i) hints (hintKey 'prices.<key>').

export const COPY = {
  'prices.title': "Prices",
  'prices.question': "What would other models and Google Cloud (Vertex) cost, and how fast are they?",
};

export const DEFS = {};
