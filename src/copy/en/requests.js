// Copy of the Requests page (owned by its page package; F0 stub).
// Every key starts with 'requests.'. COPY = labels, answers, empty states, table headers, takeaways;
// DEFS = { short, long?, template? } for (i) hints (hintKey 'requests.<key>').

export const COPY = {
  'requests.title': "Requests",
  'requests.question': "Which requests are big, which are small, and what does each one cost?",
};

export const DEFS = {};
