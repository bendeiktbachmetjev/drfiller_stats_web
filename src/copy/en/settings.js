// Copy of the Settings page (owned by its page package; F0 stub).
// Every key starts with 'settings.'. COPY = labels, answers, empty states, table headers, takeaways;
// DEFS = { short, long?, template? } for (i) hints (hintKey 'settings.<key>').

export const COPY = {
  'settings.title': "Settings",
  'settings.question': "How we count: VAT, my and test accounts, forecast assumptions, prices and data sources.",
};

export const DEFS = {};
