// Monthly form size and price since the origin ("Why did a form get more expensive", §4.3).
// SKELETON — F0-DATA implements it.

/**
 * @param {import('../buildDataset.js').Dataset} ds
 * @param {import('./scope.js').Scope} scope
 * @returns {Array<{ monthKey: string, forms: number, meanInTok: number, meanOutTok: number, meanPages: number, costPerFormEur: number }>}
 * @todo F0-DATA
 */
export function formMonthly(ds, scope) {
  return [];
}
